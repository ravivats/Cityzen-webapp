<!DOCTYPE html>
<html>
<head>
  <!-- Standard Meta -->
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">

  <!-- Site Properties -->
  <title><%= title %></title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.2.2/semantic.min.css"/>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.2.2/semantic.min.js"></script>
</head>
<body>
    <div class="ui inverted fixed menu">
        <div class="header item">
            <img class="ui tiny image" src="../images/logo.png">
        </div>
        <div class="right menu">
            <a class="item" href="../admin/analysis"><i class="signal icon"></i>View Analysis</a>
        </div>
    </div>
    <div class="ui center aligned grid" style="margin-top: 15px;" style="background-image: url('../../images/back.png'); background-size: cover; height: 100%;">
        <div class="column">
            <center>
            <div class="ui fluid center aligned segment" style="max-width: 450px;">
                <form class="ui form" action="../admin/analysis/03" method="post">
                    <div class="field">
                        <a class="ui fluid big blue button" href="../admin/analysis/05">Hot Areas</a>
                    </div>
                    <div class="field">
                        <a class="ui fluid big blue button" href="../admin/analysis/04">Public Mood Analysis</a>
                    </div>
                    <div class="field">
                        <a class="ui fluid big blue button" href="../admin/analysis/01">Complains per Category</a>
                    </div>
                    <div class="field">
                        <a class="ui fluid big blue button" href="../admin/analysis/02">Avg.Days per Category</a>
                    </div>
                    <div class="two fields">
                        <div class="field">
                            <select class="ui fluid search dropdown" name="comparepin01">
                                <% for(var i=0; i < response.length; i++) { %>
                                <option value="<%= response[i].pin %>"><%= response[i].AreaName %></option>
                                <% } %>
                            </select>
                        </div>
                        <div class="field">
                            <select class="ui fluid search dropdown" name="comparepin02">
                                <% for(var i=0; i < response.length; i++) { %>
                                <option value="<%= response[i].pin %>"><%= response[i].AreaName %></option>
                                <% } %>
                            </select>
                        </div>
                    </div>
                    <div class="field">
                        <button class="ui teal fluid submit button">Compare</button>
                    </div>
                </form>
            </div>
            </center>
        </div>
    </div>
</body>
</html>